---
description: 소스 수정 후 git 커밋 및 푸시
---

// turbo-all

1. 변경된 파일 전체 스테이징
```
git add -A
```

2. 커밋 (메시지는 작업 내용에 맞게 한글로 작성)
```
git commit -m "커밋 메시지"
```

3. main 브랜치에 푸시
```
git push origin main
```
